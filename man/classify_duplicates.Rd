% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_duplicates.R
\name{classify_duplicates}
\alias{classify_duplicates}
\title{Classify the mechanism of duplication in an ESSENCE data pull}
\usage{
classify_duplicates(
  data,
  facility_col = HospitalName,
  visit_col = Visit_ID,
  return_format = c("list", "tibble"),
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame of raw ESSENCE visit-level records.}

\item{facility_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted
column name identifying the facility. Defaults to \code{HospitalName}.
Accepts both raw ESSENCE names and post-\code{\link[janitor:clean_names]{janitor::clean_names()}}
equivalents.}

\item{visit_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted
column name identifying the visit. Defaults to \code{Visit_ID}. Accepts
both raw ESSENCE names and post-\code{\link[janitor:clean_names]{janitor::clean_names()}} equivalents.}

\item{return_format}{Character string. One of \code{"list"} (default) or
\code{"tibble"}. See Details.}

\item{verbose}{Logical. If \code{FALSE}, suppresses informational messages
(\code{rlang::inform()}); warnings and errors are always shown regardless.}
}
\value{
When \code{return_format = "list"}, a named list of class
\code{essence_dup_classified} with:
\describe{
\item{\verb{$duplicate_ids}}{Tibble of facility x visit_col pairs with
more than one row.}
\item{\verb{$visit_groups}}{Tibble with one row per facility x visit_col
containing \code{n_rows}, \code{n_biosense_ids}, \code{n_dates}, \code{n_pid},
\code{n_patient_classes} (if available), and \code{dup_type}.}
\item{\verb{$by_facility}}{Wide-format count of each \code{dup_type} per
facility, sorted by total duplicated Visit IDs descending.}
\item{\verb{$overall}}{Dataset-level counts and proportions by \code{dup_type}
via \code{\link[janitor:tabyl]{janitor::tabyl()}}.}
}
When \code{return_format = "tibble"}, the \verb{$visit_groups} tibble only.
}
\description{
Identifies and classifies the mechanism of duplication for each
facility x Visit_ID group containing more than one row.
Classification is based on which key ESSENCE fields vary within a
duplicate group: visit date (via C_BioSense_ID recomputation), patient
identifier, and/or patient class. Intended to be called before \code{\link[=dedupe]{dedupe()}}
to inform deduplication strategy and understand the nature of data quality
issues in a specific pull.
}
\details{
\subsection{Columns used}{

Required: \code{facility_col}, \code{visit_col}, \code{C_BioSense_ID}, \code{Date},
\code{C_Unique_Patient_ID}. Optional: \code{C_Patient_Class} (enables
\code{patient_class_change} detection).
}

\subsection{Duplication mechanisms in ESSENCE}{

Multiple rows for the same facility x Visit_ID arise through distinct
mechanisms, each with different causes and implications:

\describe{
\item{\code{"visit_date_change"}}{\code{C_BioSense_ID} is computed by
concatenating \code{C_Visit_Date}, \code{C_BioSense_Facility_ID} (\code{Hospital}),
and \code{C_Unique_Patient_ID}. \code{C_Visit_Date} itself takes its date value
from \code{C_Visit_Date_Time}. When a hospital's system updates
\code{C_Visit_Date_Time} as providers interact with the patient, rather
than fixing it at initial registration, and one such update crosses
midnight, \code{C_Visit_Date} changes to the next calendar day, which
recomputes \code{C_BioSense_ID} for the same \code{Visit_ID} and produces two
rows that refer to the same encounter. This is the most widespread
duplication type across NSSP sites; the affected proportion of visits
is typically small, but the impact is disproportionate in small-count
syndrome definitions where a few inflated counts can trigger anomaly
detection alerts that do not reflect genuine changes in incidence.
The duplicate arises only at facilities whose systems continue to
update \code{C_Visit_Date_Time} after initial registration. Detected by
multiple distinct \code{C_BioSense_ID} values and \code{Date} values within a
group.}
\item{\code{"pid_change"}}{\code{C_Unique_Patient_ID} (which maps to MRN in
Kentucky) is also one of the three fields concatenated into
\code{C_BioSense_ID}, so updating it mid-visit recomputes \code{C_BioSense_ID}
and produces a new row, for example, when an initial local
(facility- or department-specific) MRN is replaced with a
community-wide or health-system-level MRN, or with a different local
MRN. Detected by multiple distinct \code{C_Unique_Patient_ID} values
within a group. Not all ESSENCE sites use MRN as
\code{C_Unique_Patient_ID}.}
\item{\code{"patient_class_change"}}{Under normal HL7 processing, patient
class transitions are handled without generating duplicate rows:
the transition is appended to \code{C_Patient_Class_List} and the
\code{HasBeenE}, \code{HasBeenI}, \code{HasBeenAdmitted} flags are updated in place.
This type is detected when the same \code{Visit_ID} appears with multiple
distinct \code{c_patient_class} values, which may indicate a feed
configuration issue or a concurrent triggering event. Requires
\code{c_patient_class} to be present in the data; available via the
standard ESSENCE API. See also \code{\link[=link_encounters]{link_encounters()}} for burden
estimation from multi-class episodes.}
\item{\code{"type_unknown"}}{Multiple rows exist but all assessed fields
are identical across rows. The differentiating field was not included
in the pull. Consider adding \code{C_BioSense_ID}, \code{Date}, and
\code{C_Unique_Patient_ID} to your ESSENCE pull fields.}
\item{Compound types}{Any combination of the above joined with \code{+},
e.g., \code{"visit_date_change+pid_change"}. Indicates multiple mechanisms
acting simultaneously on the same visit. All possible combinations
of the three primary mechanisms are detected and classified.}
}
}

\subsection{Required columns}{

All classification types require \code{facility_col}, \code{visit_col},
\code{C_BioSense_ID} (or \code{c_biosense_id} / \code{c_bio_sense_id}), \code{Date} (or \code{date}), and
\code{C_Unique_Patient_ID} (or \code{c_unique_patient_id}). The function aborts
with an informative message if any are absent.
}

\subsection{Optional patient class detection}{

Detection of \code{patient_class_change} requires \code{c_patient_class} in the
data, available via the standard ESSENCE API as a pull field. When absent,
patient class change detection is skipped and all other types remain
functional.
}

\subsection{Linked vs. unlinked input}{

\code{classify_duplicates()} can be run on a raw (unlinked) ESSENCE pull, or on
the long-format output of \code{\link[=link_encounters]{link_encounters()}}: it only requires
\code{facility_col}/\code{visit_col} and the identifying columns above, regardless
of which record structure they come from. Either way, \code{patient_class_change}
(and every other type here) is detected from rows that still exist
side-by-side with a shared \code{facility_col} x \code{visit_col} key: it looks for
more than one distinct value of a field (here, \code{c_patient_class}) across
those rows, not from any single row. Once those rows have already been
collapsed into one, e.g. by \code{\link[=dedupe]{dedupe()}}, or by \code{\link[=link_encounters]{link_encounters()}} with
\code{return_format = "collapsed"}, there is only one row left per key, so
\code{n_rows == 1} and nothing is classified as a duplicate at all, regardless
of what mechanism originally produced the now-merged rows. Classify before
collapsing if you want to see which mechanism was responsible.
}

\subsection{Return formats}{

\describe{
\item{\code{"list"} (default)}{A named list of class \code{essence_dup_classified}
with \verb{$duplicate_ids}, \verb{$visit_groups}, \verb{$by_facility}, and
\verb{$overall}. \verb{$duplicate_ids} is a tibble of facility x visit_col
pairs with more than one row, consistent with
\code{\link[=summarize_duplicates]{summarize_duplicates()}}\verb{$duplicate_ids}.}
\item{\code{"tibble"}}{The visit-group level summary tibble only: one row
per facility x visit_col with \code{dup_type} and supporting \verb{n_*}
metrics. Suitable for joining back to original data or piping into
further analysis.}
}
}
}
\examples{
# Default list return
essence_raw |> classify_duplicates()

# Tibble return for piping
essence_raw |>
  classify_duplicates(return_format = "tibble") |>
  dplyr::filter(dup_type == "visit_date_change")

# Join classifications back to raw data for row-level inspection
# (clean first so join keys match the snake_case output of classify_duplicates)
essence_raw |>
  janitor::clean_names() |>
  dplyr::left_join(
    classify_duplicates(essence_raw, return_format = "tibble"),
    by = c("hospital_name", "visit_id")
  )

}
\seealso{
\code{\link[=summarize_duplicates]{summarize_duplicates()}} for counts without mechanism detail;
\code{\link[=dedupe]{dedupe()}} to remove duplicates after review.
}
