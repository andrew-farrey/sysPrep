% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review_facility_ed_visits.R
\name{review_facility_ed_visits}
\alias{review_facility_ed_visits}
\title{Review facility ED visit counts for data quality assessment}
\usage{
review_facility_ed_visits(
  data,
  facility_col = HospitalName,
  facility_type_col = FacilityType,
  date_col = NULL,
  method = c("percentile", "iqr", "both"),
  percentile_low = 0.05,
  percentile_high = 0.95,
  iqr_multiplier = 1.5,
  group_by_type = FALSE,
  return_format = c("outliers_only", "all"),
  clean_names = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame of ESSENCE visit-level records, typically after
\code{\link[=dedupe]{dedupe()}} and optionally \code{\link[=filter_care_setting]{filter_care_setting()}}.}

\item{facility_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted
column name identifying the facility. Defaults to \code{HospitalName}.}

\item{facility_type_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}>
Unquoted column name identifying the facility type. Defaults to
\code{FacilityType}.}

\item{date_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Optional.
Unquoted column name for the visit date. When supplied, outlier
detection uses visits per day rather than raw counts. Accepts \code{Date},
\code{C_Visit_Date}, or post-\code{\link[janitor:clean_names]{janitor::clean_names()}} equivalents.
Defaults to \code{NULL}.}

\item{method}{Character string. Outlier detection method. One of
\code{"percentile"} (default), \code{"iqr"}, or \code{"both"}. See Details.}

\item{percentile_low}{Numeric. Lower percentile threshold for
\code{method = "percentile"} or \code{"both"}. Defaults to \code{0.05}.}

\item{percentile_high}{Numeric. Upper percentile threshold for
\code{method = "percentile"} or \code{"both"}. Defaults to \code{0.95}.}

\item{iqr_multiplier}{Numeric. Fence multiplier for \code{method = "iqr"} or
\code{"both"}. Defaults to \code{1.5} (Tukey standard). Increase to reduce
sensitivity; decrease to increase sensitivity to dropouts.}

\item{group_by_type}{Logical. If \code{TRUE}, outlier thresholds are computed
separately within each \code{FacilityType} group. Defaults to \code{FALSE}.}

\item{return_format}{Character string. One of \code{"outliers_only"} (default)
or \code{"all"}. \code{"outliers_only"} returns only flagged facilities.
\code{"all"} returns all facilities with outlier flag columns appended.}

\item{clean_names}{Logical. If \code{TRUE} (default), applies
\code{\link[janitor:clean_names]{janitor::clean_names()}} to standardize column names on output.}

\item{verbose}{Logical. If \code{FALSE}, suppresses informational messages
(\code{rlang::inform()}); warnings and errors are always shown regardless.}
}
\value{
A tibble with one row per facility containing facility identifier,
facility type, \code{n_visits}, optionally \code{visits_per_day} and \code{n_days},
and outlier flag columns \code{.outlier_low}, \code{.outlier_high},
\code{.outlier_flag}, \code{.outlier_direction}, and \code{.outlier_method}.
}
\description{
Computes visit counts per facility and flags statistical outliers --
facilities with disproportionately low or high visit volumes relative to
the pull as a whole. Intended as an ongoing data quality monitoring tool
for production surveillance pipelines, where unexpectedly low counts may
indicate feed outages, facility onboarding issues, or non-ED providers,
and unexpectedly high counts may indicate feed duplication or query
misconfiguration.
}
\details{
\subsection{Interpreting results}{

Low-count outliers are expected and normal for narrow syndrome definitions,
particularly at small rural hospitals. A rural ED with two opioid overdose
visits in a given month is not a data quality problem -- it may reflect
true low incidence. This function is best used on \strong{denominator data}
(all-cause ED visits) where facility volumes are stable and comparable.
When applied to narrow case definitions, some ED facilities will routinely
appear as low outliers. The function is intended to make routine QA
assessment easier, not to drive exclusion decisions. Always review
flagged facilities in context.
}

\subsection{Outlier methods}{

\describe{
\item{\code{"percentile"} (default)}{Flags facilities below
\code{percentile_low} and above \code{percentile_high} of the visit count
distribution. Default thresholds of 5th and 95th percentile are
interpretable and robust to skewed count distributions.}
\item{\code{"iqr"}}{Flags facilities below \code{Q1 - iqr_multiplier * IQR} and
above \code{Q3 + iqr_multiplier * IQR}. Tukey fences with the default
multiplier of 1.5 are sensitive to data dropouts -- a facility that
abruptly stops sending visits during a pull window will fall well
below the lower fence. Increase \code{iqr_multiplier} to reduce
sensitivity.}
\item{\code{"both"}}{Applies both methods independently. A facility is
flagged if it meets either criterion. \code{.outlier_method} indicates
which method triggered.}
}
}

\subsection{Time normalization}{

When \code{date_col} is supplied, visits per day is computed and outlier
detection is applied to the normalized rate rather than raw counts.
This makes pulls of different durations comparable and improves
sensitivity to within-pull data dropouts (e.g., a facility that stops
sending data mid-month).
}

\subsection{Grouped detection}{

When \code{group_by_type = TRUE}, outlier thresholds are computed separately
within each \code{FacilityType}. This prevents large academic medical centers
from inflating thresholds that mask low counts at smaller facilities.
}
}
\examples{
# Default: percentile method, outliers only
essence_clean |> review_facility_ed_visits()

# All facilities with flags
essence_clean |> review_facility_ed_visits(return_format = "all")

# IQR method with time normalization for dropout detection
essence_clean |>
  review_facility_ed_visits(
    method   = "iqr",
    date_col = Date
  )

# Both methods, grouped by facility type
essence_clean |>
  review_facility_ed_visits(
    method        = "both",
    group_by_type = TRUE,
    return_format = "all"
  )

}
\seealso{
\code{\link[=filter_care_setting]{filter_care_setting()}} for removing non-ED facilities before review.
}
