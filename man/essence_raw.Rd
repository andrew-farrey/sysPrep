% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{essence_raw}
\alias{essence_raw}
\title{Synthetic raw ESSENCE-like ED visit data}
\format{
A data frame with approximately 197 rows and 19 columns:
\describe{
\item{HospitalName}{Character. Synthetic facility name.}
\item{Hospital}{Integer. Synthetic numeric facility identifier
(\code{C_BioSense_Facility_ID} equivalent).}
\item{FacilityType}{Character. ESSENCE facility type. Includes
intentional misclassifications for two FSEDs (\code{"Urgent Care"}) to
demonstrate \code{\link[=filter_care_setting]{filter_care_setting()}} correction logic.}
\item{HospitalRegion}{Character. Facility ESSENCE Region in
\verb{\{SITE\}_\{REGION\}} format (e.g., \code{"KY_Jefferson"}).}
\item{HospitalZip}{Character. Facility zip code.}
\item{Visit_ID}{Character. Synthetic visit identifier, unique within
a facility. Multiple rows with the same \verb{HospitalName x Visit_ID}
represent the duplicate patterns this package addresses.}
\item{C_BioSense_ID}{Character. Synthetic BioSense record identifier.
Multiple values per \code{Visit_ID} indicate \code{visit_date_change}
duplication.}
\item{C_Unique_Patient_ID}{Character. Synthetic patient identifier
(MRN equivalent in Kentucky). Multiple values per \code{Visit_ID}
indicate \code{pid_change} duplication.}
\item{Date}{Date. Visit date.}
\item{C_Visit_Date_Time}{POSIXct. Timestamp of the actual clinical
encounter. Used by \code{\link[=link_encounters]{link_encounters()}} to chronologically order
\code{.patient_class_sequence} when linking ED and direct-admit records.}
\item{Arrived_Date_Time}{POSIXct. Timestamp when NSSP received the
record. Use as \code{order_by} in \code{\link[=dedupe]{dedupe()}} to retain the most recently
transmitted version of each record.}
\item{HasBeenE}{Integer. \code{1} for \code{PullSource = "ED"} rows, consistent
with a \code{HasBeenE = 1} filtered ESSENCE pull. \code{0} on all
\code{PullSource = "Admission"} rows (see Details): the two
\code{patient_class_change} duplicate rows, which represent a
mis-submitted direct-admit continuation of an ED visit, and four
genuine direct admissions with no preceding ED visit at all.}
\item{HasBeenAdmitted}{Integer. \code{1} if the visit resulted in
inpatient admission (discharge-disposition aware).}
\item{C_Patient_Class}{Character. ESSENCE-derived single-letter patient
class (\code{"E"} for ED, \code{"I"} for inpatient). \code{classify_duplicates()}
detects \code{patient_class_change} duplicates generically, from any two
rows sharing a \verb{facility x Visit_ID} key with distinct
\code{C_Patient_Class} values -- not a specific from/to pair. \code{"E"} to
\code{"I"} is used here only as a common, realistic illustration; see
Details.}
\item{Region}{Character. Patient ESSENCE Region of residence in
\verb{\{SITE\}_\{REGION\}} format. Includes out-of-state values
(e.g., \code{"TN_Davidson"}) and \code{"OTHER_REGION"} entries to demonstrate
\code{\link[=assign_treating_geography]{assign_treating_geography()}}.}
\item{ZipCode}{Character. Patient zip code. May be \code{NA} for
\code{OTHER_REGION} records.}
\item{Sex}{Character. \code{"M"}, \code{"F"}, or \code{"U"} (unknown).}
\item{C_Patient_Age}{Integer. Patient age in years.}
\item{PullSource}{Character, \code{"ED"} or \code{"Admission"}. Not a real
ESSENCE field -- marks which of two separately-run ESSENCE queries
(\code{HasBeenE = 1} vs. \code{HasBeenAdmitted = 1}) this row would have come
back in, so \code{vignette("encounter-linkage")} can split \code{essence_raw}
back into the two pulls \code{\link[=link_encounters]{link_encounters()}} is designed to
recombine. Drop this column (or filter and discard it) before
treating a subset of \code{essence_raw} as a real single ESSENCE pull.}
}
}
\source{
Entirely synthetic. Generated by \code{data-raw/generate_synthetic_data.R}.
Column structure models an ESSENCE \code{va_er} (Patient Location, Full
Details) pull, the data source against which \code{sysPrep}'s functions
have been validated. Encounter linkage (\code{link_encounters()}) expects a
supplemental inpatient pull structured as \code{va_hosp} (Facility Location,
Full Details).
}
\usage{
essence_raw
}
\description{
A fabricated dataset simulating a raw ESSENCE API data pull containing
common data quality issues: duplicate records of multiple types, non-ED
provider visits, out-of-state patient records, and \code{OTHER_REGION} entries.
All facility names, visit identifiers, patient identifiers, and geographic
values are entirely synthetic. No real patient or facility data are included.
}
\details{
\subsection{Duplicate patterns included}{

\describe{
\item{\code{standard}}{5 visits with an extra row differing only in
\code{Arrived_Date_Time} -- representing a record retransmission.}
\item{\code{visit_date_change}}{3 visits with a second row carrying a new
\code{C_BioSense_ID} and a \code{Date} advanced by 1 day -- representing a
midnight-crossing visit where \code{Admit_Date_Time} was updated.}
\item{\code{pid_change}}{2 visits with a second row carrying a different
\code{C_Unique_Patient_ID} -- representing a corrected patient identifier.}
\item{\code{patient_class_change}}{2 visits with a second row carrying
\code{C_Patient_Class = "I"} (\code{HasBeenE}/\code{HasBeenAdmitted} flipped to
match) against the original row's \code{"E"}, and later \code{C_Visit_Date_Time}/
\code{Arrived_Date_Time} values -- representing an ED visit and its
direct-admit continuation transmitted as two records sharing one
\code{Visit_ID} rather than one record with an updated
\code{C_Patient_Class_List}. Since \code{dedupe(keep = "last")} keeps the more
recent row by \code{Arrived_Date_Time}, these two visits survive into
\link{essence_clean} as \code{c_patient_class = "I"} -- the ED encounter is
silently dropped by deduplication alone, illustrating the gap
\code{link_encounters()} is designed to catch instead. These are also the
\code{PullSource = "Admission"} rows that share a \verb{HospitalName x Visit_ID} key with a real \code{PullSource = "ED"} row -- see
\code{PullSource} above.}
\item{\code{visit_date_change+pid_change}}{1 visit exhibiting both
mechanisms simultaneously.}
}
}

\subsection{Genuine direct admissions}{

4 additional visits with \code{HasBeenE = 0}, \code{HasBeenAdmitted = 1}, and a
\code{Visit_ID} that appears nowhere else in \code{essence_raw} -- true direct
admissions with no preceding ED visit at all (\code{PullSource = "Admission"}), structurally invisible to a \code{HasBeenE = 1} query and
distinct from the \code{patient_class_change} rows above, which share a key
with a real ED row. See \code{vignette("encounter-linkage")}.
}

\subsection{Non-ED providers}{

20 records from a \code{"Primary Care"} and a \code{"Medical Specialty"} facility
are included. These should be excluded by \code{\link[=filter_care_setting]{filter_care_setting()}}.
}

\subsection{FSED misclassifications}{

\code{"Hillside FSED"} and \code{"Downtown Emergency Services"} are onboarded with
\code{FacilityType = "Urgent Care"}. These require correction via
\code{fix_facility_type_vector} in \code{\link[=filter_care_setting]{filter_care_setting()}}.
}
}
\seealso{
\link{essence_clean} for the processed version of this dataset;
\code{\link[=dedupe]{dedupe()}}, \code{\link[=filter_care_setting]{filter_care_setting()}}, \code{\link[=assign_treating_geography]{assign_treating_geography()}},
\code{\link[=link_encounters]{link_encounters()}} for how \code{PullSource} is used to demonstrate
two-pull encounter linkage.
}
\keyword{datasets}
