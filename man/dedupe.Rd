% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dedupe.R
\name{dedupe}
\alias{dedupe}
\title{Remove duplicate records from an ESSENCE data pull}
\usage{
dedupe(
  data,
  facility_col = NULL,
  visit_col = Visit_ID,
  order_by = NULL,
  keep = "first",
  clean_names = TRUE
)
}
\arguments{
\item{data}{A data frame of raw ESSENCE visit-level records.}

\item{facility_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted
column name identifying the facility. When not supplied, prefers
\code{Hospital}/\code{C_BioSense_Facility_ID} over \code{HospitalName} if present;
see Details. Accepts both raw ESSENCE names and
post-\code{\link[janitor:clean_names]{janitor::clean_names()}} equivalents.}

\item{visit_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted
column name identifying the visit. Defaults to \code{Visit_ID}. Common
alternatives include \code{MedicalRecordNumber}, \code{MRN}, \code{VisitNumber}, and
\code{C_Unique_Patient_ID}. Accepts both raw ESSENCE names and
post-\code{\link[janitor:clean_names]{janitor::clean_names()}} equivalents.}

\item{order_by}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Optional.
Unquoted column name to sort by within each facility x visit group
before applying \code{keep}. Ignored when \code{keep = "most_complete"} (a warning
is issued). Use \code{Arrived_Date_Time} to retain the most recently
transmitted record (\code{keep = "last"}) or the earliest (\code{keep = "first"}).
Other useful options: \code{C_Visit_Date}, \code{C_Visit_Date_Time}, \code{Date}.
Defaults to \code{NULL} (row order as received).}

\item{keep}{Character string. Which row to retain per group. One of
\code{"first"} (default), \code{"last"}, or \code{"most_complete"}. See Details.}

\item{clean_names}{Logical. If \code{TRUE} (default), applies
\code{\link[janitor:clean_names]{janitor::clean_names()}} to standardize column names to snake_case
after deduplication.}
}
\value{
A deduplicated data frame with one row per \code{facility_col} x
\code{visit_col} combination.
}
\description{
Retains one row per unique facility x visit identifier combination.
ESSENCE data frequently contains multiple rows for the same visit due to
query overlap, multi-facility pulls, or late-arriving record updates.
This function formalizes the deduplication step prior to case counting,
cluster detection, or geographic attribution.
}
\details{
\subsection{Why deduplication is necessary}{

The ESSENCE API may return multiple rows for a single facility x Visit_ID
combination due to several mechanisms: standard data feed retransmissions,
midnight-crossing visits that trigger recomputation of C_BioSense_ID,
patient identifier corrections mid-visit, and patient class transitions.
Without deduplication, visit counts, rates, and cluster detection outputs
are inflated. See \code{\link[=classify_duplicates]{classify_duplicates()}} to understand the mechanism of
duplication in a specific pull before deduplicating.
}

\subsection{Deduplication key}{

The deduplication key is always \verb{facility_col x visit_col}. Visit_ID is
unique within a facility in ESSENCE; the same Visit_ID at two different
facilities represents two distinct encounters and is not collapsed.
}

\subsection{Missing key values are never treated as duplicates}{

A row with a missing \code{facility_col} or \code{visit_col} value has an unknown
identity, not one confirmed to match every other row with a missing
value. \code{dedupe()} never groups two such rows together, even when they
share the same facility and both have a missing \code{visit_col}: each is
retained as its own distinct row. This differs from grouping directly
on the raw columns (e.g. \code{dplyr::group_by()}), which follows SQL's
\verb{GROUP BY} convention of treating every \code{NA} as equal to every other
\code{NA} and would otherwise silently discard genuinely distinct visits
whose identifier happened to be missing. \code{rlang::inform()} reports how
many rows were affected whenever this occurs.
}

\subsection{Facility identifier preference}{

When \code{facility_col} is not explicitly supplied, \code{dedupe()} prefers
\code{Hospital}/\code{C_BioSense_Facility_ID} (a stable numeric facility
identifier) over \code{HospitalName} whenever it's present in the data,
falling back to \code{HospitalName} only if \code{Hospital} isn't available. A
facility rename or rebrand changes \code{HospitalName} but not \code{Hospital};
deduplicating by name can silently split what should be one facility's
rows across a rename, or merge two different facilities that briefly
share a display name. Explicitly passing \code{facility_col} (either
column) always overrides this preference exactly as given.
}

\subsection{keep strategies}{

\describe{
\item{\code{"first"} (default)}{Retains the first row as received. When
\code{order_by} is supplied, retains the earliest record by that column.
Fastest and most transparent.}
\item{\code{"last"}}{Retains the final row. When \code{order_by} is supplied,
retains the most recently received record; appropriate when ESSENCE
records are updated chronologically and later rows reflect corrected
information. Use \code{Arrived_Date_Time} as \code{order_by} to retain the most
recently transmitted version of each record.}
\item{\code{"most_complete"}}{Retains the row with the fewest \code{NA} values
across all columns. Useful when records vary in completeness due to
late-arriving lab or disposition fields. \code{order_by} is ignored and a
warning is issued if supplied.}
}
}

\subsection{Column name flexibility}{

All \verb{_col} arguments accept both raw ESSENCE column names (e.g.,
\code{HospitalName}) and post-\code{\link[janitor:clean_names]{janitor::clean_names()}} equivalents (e.g.,
\code{hospital_name}). The function normalizes both the supplied name and the
data's column names to snake_case for matching, then returns results using
the actual column names present in the data.
}
}
\examples{
# Default: one row per Hospital x Visit_ID (Hospital is preferred over
# HospitalName since essence_raw has it), first row as received
essence_raw |> dedupe()

# Retain earliest record by visit date
essence_raw |> dedupe(order_by = Date, keep = "first")

# Retain most recently transmitted record (best for rolling pulls)
essence_raw |> dedupe(order_by = Arrived_Date_Time, keep = "last")

# Retain most complete record per visit
essence_raw |> dedupe(keep = "most_complete")

# Use numeric facility ID instead of name
essence_raw |> dedupe(facility_col = Hospital)

# Deduplicate by patient identifier (MRN-equivalent in ESSENCE)
essence_raw |> dedupe(visit_col = C_Unique_Patient_ID)

# Works with post-clean_names() column names too
essence_raw |>
  janitor::clean_names() |>
  dedupe(order_by = arrived_date_time, keep = "last")

# Full recommended pre-processing pipeline
essence_raw |>
  dedupe(order_by = Arrived_Date_Time, keep = "last") |>
  filter_care_setting() |>
  assign_treating_geography()

}
\seealso{
\code{\link[=summarize_duplicates]{summarize_duplicates()}} to count duplicates before deduplication;
\code{\link[=classify_duplicates]{classify_duplicates()}} to understand duplication mechanisms.
}
