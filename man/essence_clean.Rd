% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{essence_clean}
\alias{essence_clean}
\title{Synthetic cleaned ESSENCE-like ED visit data}
\format{
A data frame with approximately 160 rows and 21 columns. All
column names are in snake_case (post \code{\link[janitor:clean_names]{janitor::clean_names()}}).
\describe{
\item{hospital_name}{Character. Synthetic facility name.}
\item{hospital}{Integer. Synthetic numeric facility identifier.}
\item{facility_type}{Character. ESSENCE facility type. Non-ED
providers have been removed; FSED corrections applied.}
\item{hospital_region}{Character. Facility ESSENCE Region in
\verb{\{SITE\}_\{REGION\}} format.}
\item{hospital_zip}{Character. Facility zip code.}
\item{visit_id}{Character. Synthetic visit identifier, unique within
a facility.}
\item{c_bio_sense_id}{Character. Synthetic BioSense record identifier.
One unique value per visit after deduplication.}
\item{c_unique_patient_id}{Character. Synthetic patient identifier.}
\item{date}{Date. Visit date.}
\item{c_visit_date_time}{POSIXct. Timestamp of the actual clinical
encounter.}
\item{arrived_date_time}{POSIXct. NSSP record receipt timestamp.}
\item{has_been_e}{Integer. \code{1} for an ED pull; \code{0} on the two visits
where a \code{patient_class_change} direct-admit row outranked the ED row
under \code{dedupe(keep = "last")} -- see \code{c_patient_class} and
\code{?essence_raw}.}
\item{has_been_admitted}{Integer. \code{1} if the visit resulted in
inpatient admission.}
\item{c_patient_class}{Character. ESSENCE-derived single-letter patient
class. \code{"I"} on the two visits where deduplication kept the
direct-admit continuation row over the original ED row (see
\code{?essence_raw}); \code{"E"} otherwise. Unchanged by the preprocessing
pipeline.}
\item{region}{Character. Patient ESSENCE Region of residence in
\verb{\{SITE\}_\{REGION\}} format. Out-of-state and \code{OTHER_REGION} visits have
been reassigned to the treating facility's Region by
\code{\link[=assign_treating_geography]{assign_treating_geography()}}.}
\item{zip_code}{Character. Patient zip code (reassigned for
out-of-state visits).}
\item{sex}{Character. \code{"M"}, \code{"F"}, or \code{"U"}.}
\item{c_patient_age}{Integer. Patient age in years.}
\item{original_region}{Character. Pre-reassignment \code{Region} value,
preserved when \code{preserve_original_geographies = TRUE}.}
\item{original_zip_code}{Character. Pre-reassignment \code{ZipCode} value.}
\item{.out_of_state}{Logical. \code{TRUE} for visits where \code{Region} and/or
\code{ZipCode} were reassigned to treating facility geography.}
}
}
\source{
Derived from \link{essence_raw} via
\code{data-raw/generate_synthetic_data.R}.
}
\usage{
essence_clean
}
\description{
The result of running \code{essence_raw} through the full \code{sysPrep} preprocessing
pipeline: deduplication, care setting filtering with FSED correction, and
treating geography assignment for out-of-state visits. Intended to
demonstrate the expected output of the package workflow and to serve as
a reference for function output structure.
}
\seealso{
\link{essence_raw} for the raw version; the Getting Started vignette
for the full pipeline demonstration.
}
\keyword{datasets}
