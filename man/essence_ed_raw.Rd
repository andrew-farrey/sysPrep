% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{essence_ed_raw}
\alias{essence_ed_raw}
\title{Synthetic raw ESSENCE-like ED pull for encounter linkage}
\format{
A data frame with approximately 15 rows and 19 columns. Same
column structure as \link{essence_raw}, plus \code{HasBeenI} (see Details); see
\link{essence_raw}'s documentation for the other column definitions.
}
\source{
Entirely synthetic. Generated by \code{data-raw/generate_synthetic_data.R}.
}
\usage{
essence_ed_raw
}
\description{
A small, fabricated dataset representing a \code{HasBeenE = 1} ESSENCE query,
purpose-built to demonstrate \code{\link[=link_encounters]{link_encounters()}} two-pull linkage in
\code{vignette("encounter-linkage")} alongside \link{essence_inp_raw}. Unlike
\link{essence_raw} (a single, larger, realistic pull covering every
deduplication/care-setting/geography issue \code{sysPrep} addresses),
\code{essence_ed_raw} exists solely to demonstrate encounter linkage and is
deliberately small. All facility names, visit identifiers, patient
identifiers, and geographic values are entirely synthetic. No real
patient or facility data are included.
}
\details{
\subsection{Visit types included}{

\describe{
\item{Plain ED-only visits}{\code{HasBeenE = 1}, \code{HasBeenAdmitted = 0} --
the majority of rows.}
\item{Correctly carried-through escalation}{\code{HasBeenE = 1} and
\code{HasBeenAdmitted = 1} on one already-deduplicated record -- needs no
cross-pull linking, since there is only ever one record. These 2
visits also have \code{HasBeenI = 1}, so that
\code{link_encounters()}'s preference for \code{HasBeenAdmitted} over
\code{HasBeenI} when both are present is demonstrable with real data.}
\item{Continuity-break ED half}{2 rows that look like plain ED visits
within \code{essence_ed_raw} alone (\code{HasBeenE = 1}, \code{HasBeenAdmitted = 0});
their mis-submitted direct-admit continuation lives in
\link{essence_inp_raw}, sharing the same \verb{HospitalName x Visit_ID} key.
\code{link_encounters()} should merge each pair back into one episode.}
}

One standard retransmission duplicate (same \verb{HospitalName x Visit_ID},
differing \code{Arrived_Date_Time}) is included so
\code{vignette("encounter-linkage")}'s "dedupe each pull separately before
linking" guidance has something real to demonstrate.
}
}
\seealso{
\link{essence_inp_raw}, the paired inpatient-admission pull;
\link{essence_raw} for the general-purpose synthetic pull used elsewhere;
\code{\link[=link_encounters]{link_encounters()}} and \code{vignette("encounter-linkage")}.
}
\keyword{datasets}
