% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_facility_geography.R
\name{assign_facility_geography}
\alias{assign_facility_geography}
\title{Assign facility geography to all visits for incidence-at-location analysis}
\usage{
assign_facility_geography(
  data,
  region_col = Region,
  hospital_region_col = HospitalRegion,
  zip_col = ZipCode,
  hospital_zip_col = HospitalZip,
  new_region_col = "region_facility",
  new_zip_col = "zip_code_facility",
  overwrite = FALSE,
  preserve_original_geographies = FALSE,
  clean_names = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame of ESSENCE visit-level records, typically the
output of \code{\link[=dedupe]{dedupe()}} and \code{\link[=filter_care_setting]{filter_care_setting()}}.}

\item{region_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted
column name for the patient region field. Defaults to \code{Region}.}

\item{hospital_region_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}>
Unquoted column name for the hospital region field. Defaults to
\code{HospitalRegion}. Required when \code{new_region_col} is not \code{NULL}.}

\item{zip_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted
column name for the patient zip code field. Defaults to \code{ZipCode}.
Required when \code{new_zip_col} is not \code{NULL}.}

\item{hospital_zip_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}>
Unquoted column name for the hospital zip code field. Defaults to
\code{HospitalZip}. Required when \code{new_zip_col} is not \code{NULL}.}

\item{new_region_col}{Character string or \code{NULL}. Name of the column to
write facility region values to. Defaults to \code{"region_facility"} -- a
new column, leaving \code{region_col} untouched. Pass \code{NULL} to skip region
entirely. Pass \code{region_col}'s own name (with \code{overwrite = TRUE}) to
overwrite it in place instead. See Details.}

\item{new_zip_col}{Character string or \code{NULL}. Name of the column to
write facility zip code values to. Defaults to \code{"zip_code_facility"}.
Same behavior as \code{new_region_col}, independently, for zip.}

\item{overwrite}{Logical. If \code{FALSE} (default), \code{new_region_col}/
\code{new_zip_col} naming a column that already exists in \code{data} aborts
rather than silently overwriting it. Set \code{TRUE} to allow it -- this is
required to reproduce the original in-place-overwrite behavior. See
Details.}

\item{preserve_original_geographies}{Logical. If \code{TRUE}, adds
\code{original_region} and/or \code{original_zip_code} columns before
overwriting. Only has an effect when \code{overwrite = TRUE}. Defaults to
\code{FALSE}.}

\item{clean_names}{Logical. If \code{TRUE} (default), applies
\code{\link[janitor:clean_names]{janitor::clean_names()}} on output.}

\item{verbose}{Logical. If \code{FALSE}, suppresses informational messages
(\code{rlang::inform()}); warnings and errors are always shown regardless.}
}
\value{
The input data frame with facility region and/or zip code values
written to \code{new_region_col}/\code{new_zip_col} (or to \code{region_col}/
\code{zip_col} in place, when \code{overwrite = TRUE}) for all rows. A
\code{.facility_geography} logical column set to \code{TRUE} for all rows signals
that facility geography has been applied. When \code{overwrite = TRUE} and
\code{preserve_original_geographies = TRUE}, \code{original_region} and/or
\code{original_zip_code} columns are also present.
}
\description{
Writes the treating hospital's corresponding geographic fields
(\code{HospitalRegion} and/or \code{HospitalZip}) to \code{new_region_col}/\code{new_zip_col}
for \strong{all} visits, regardless of patient residence. By default this
writes new columns (\code{region_facility}/\code{zip_code_facility}), leaving
\code{Region}/\code{ZipCode} untouched; set \code{overwrite = TRUE} to overwrite them in
place instead. This scopes geography to true incidence-at-location (to
the extent NSSP ESSENCE supports it) -- by attributing every visit to
where care was received rather than where the patient resides.
}
\details{
\subsection{Why this function exists}{

\code{\link[=assign_treating_geography]{assign_treating_geography()}} selectively reassigns geography only for
out-of-state and \code{OTHER_REGION} visits, preserving residential geography
for in-state patients -- a hybrid approach. \code{assign_facility_geography()}
applies reassignment universally: all visits, including in-state patients,
receive the treating facility's geography. This produces a dataset scoped
entirely to incidence-at-location rather than patient residence.

This is functionally equivalent to renaming \code{HospitalRegion} to \code{Region}
and \code{HospitalZip} to \code{ZipCode}, but operates on the existing data structure
without column renaming or pipeline restructuring, preserving compatibility
with downstream functions that expect \code{Region} and \code{ZipCode} by name.
}

\subsection{Incidence-at-location scoping and region-level cluster detection}{

Assigning facility geography to all visits bins every visit to the region
in which the treating facility is located, regardless of how many hospitals
that region contains. This is a deliberate design choice with two advantages
over true hospital-level cluster detection:
\itemize{
\item \strong{Reduces urban-hospital bias.} Urban regions typically contain more
hospitals than rural regions. Hospital-level scan statistics can evaluate
clusters across individual facility locations, but they generally do not
explicitly model residual spatial autocorrelation or facility-specific
reporting patterns as nuisance structure. In prospective surveillance,
scanning windows may therefore interact with hospital density, catchment
patterns, and data submission variability in ways that can produce apparent
clusters independent of true changes in underlying incidence. This concern
is more pronounced in urban areas where hospitals are denser and submission
patterns may vary across nearby facilities. Region-level binning does not
eliminate this concern, but it reduces facility-level noise by aggregating
across treating facilities within a region rather than treating each
hospital as an independent spatial unit.
\item \strong{No Master Facility Table lookup required.} Hospital-level spatial
clustering requires latitude/longitude coordinates sourced from the NSSP
Master Facility Table, an additional logistical step outside the standard
ESSENCE data pull. Region-level binning achieves a close approximation of
hospital-level cluster detection using \code{HospitalRegion}/\code{HospitalZip}
instead -- provided they're included in the pull. Neither is guaranteed
by default; both require being part of your site's default returned
columns or explicitly requested via the API's \verb{&field=} parameter.
}

One consequence of region-level attribution is that detectable cluster
polygons are limited to regions that contain at least one participating
ESSENCE facility. Regions with no participating hospitals cannot contribute
counts and will not appear as cluster candidates, regardless of actual
burden in those areas. This is not unique to this approach -- it is a
general property of facility-based surveillance -- but it is worth noting
explicitly when interpreting spatial cluster output.

This method is currently being evaluated for adoption as an additional
scan statistic pipeline for daily prospective overdose surveillance in
Kentucky, complementing the hybrid approach implemented in
\code{\link[=assign_treating_geography]{assign_treating_geography()}}.
}

\subsection{Use cases}{
\itemize{
\item Spatial cluster detection scoped to where patients received care rather
than where they live.
\item Incidence-at-location burden estimation irrespective of patient origin.
\item Approximating hospital-level cluster detection without Master Facility
Table lookups or coordinate-based spatial methods.
\item Comparing total treated volume across urban and rural counties without
differentiating which specific hospital saw each visit -- useful when a
facility-to-county lookup table isn't available but \code{HospitalRegion}/
\code{HospitalZip} already are. The tradeoff is the one described above: a
county with no participating ESSENCE facility always shows \code{0},
regardless of actual resident burden.
}
}

\subsection{ESSENCE Region field and facility location pulls}{

\code{HospitalRegion}, like \code{Region}, is drawn from a maintained zip-to-county
mapping rather than a live geocode -- by default assigned from the
facility's zip code centroid, though site administrators can override
this via BioSense Access Management to instead reflect the facility's
listed county. \code{Region}-based output should not be construed as an
authoritative county-level count for either patient or facility geography.

This function's output is best understood as approximating an ESSENCE
\strong{facility location} pull (\code{va_hosp}): every row is scoped to where care
was received, so the resulting population is a combination of
jurisdiction residents and visitors who presented at in-jurisdiction
facilities. This differs from a \strong{patient location} pull (\code{va_er}),
which \code{\link[=assign_treating_geography]{assign_treating_geography()}} is closer to preserving -- see that
function's documentation for the same caveat on \code{Region} field
interpretation.
}

\subsection{Geography types and output columns}{

Both region and zip are attempted by default (\code{new_region_col} and
\code{new_zip_col} both default to a column name). Pass \code{NULL} to either to
skip that type entirely. If the source columns required for a requested
type are absent, that type is skipped with an informative message.

By default, results are written to new columns (\code{region_facility}/
\code{zip_code_facility}), and \code{region_col}/\code{zip_col} are never modified. To
overwrite \code{region_col}/\code{zip_col} in place instead (the only behavior this
function had before this parameter existed), pass their own name to
\code{new_region_col}/\code{new_zip_col} and set \code{overwrite = TRUE}. \code{overwrite}
guards \strong{any} collision with an existing column, not just the source
column -- see \code{\link[=assign_treating_geography]{assign_treating_geography()}}'s documentation for the full
behavior, which is identical here.
}

\subsection{Preserved geographies}{

\code{preserve_original_geographies} only has an effect in \code{overwrite = TRUE}
mode -- see \code{\link[=assign_treating_geography]{assign_treating_geography()}}. When it does apply here, since
all rows are reassigned, \code{original_region}/\code{original_zip_code} retain the
original patient residential geography for every visit, enabling
residential vs. treating geography comparisons in a single dataset.
}
}
\examples{
# Build a deduplicated, filtered base to demonstrate on -- essence_clean
# itself already has region_hybrid/region_facility applied, so it isn't a
# fresh starting point for these examples
ed_clean <- essence_raw |>
  dedupe(order_by = Arrived_Date_Time) |>
  filter_care_setting()

# Default: new region_facility/zip_code_facility columns for all visits;
# region/zip_code themselves are never touched
ed_clean |> assign_facility_geography()

# Overwrite region/zip_code in place (the original behavior) -- requires
# naming the source columns explicitly and opting in with overwrite
ed_clean |>
  assign_facility_geography(
    new_region_col = "region",
    new_zip_col     = "zip_code",
    overwrite       = TRUE,
    preserve_original_geographies = TRUE
  ) |>
  dplyr::mutate(
    patient_differs_from_facility = region != original_region
  )

# Facility geography for cluster detection, alongside the hybrid approach
ed_clean |>
  assign_treating_geography() |>
  assign_facility_geography()

}
\seealso{
\code{\link[=assign_treating_geography]{assign_treating_geography()}} for selective reassignment of
out-of-state visits only.
}
