% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_duplicates.R
\name{summarize_duplicates}
\alias{summarize_duplicates}
\title{Summarize duplicate records in an ESSENCE data pull}
\usage{
summarize_duplicates(data, facility_col = HospitalName, visit_col = Visit_ID)
}
\arguments{
\item{data}{A data frame of raw ESSENCE visit-level records.}

\item{facility_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted
column name identifying the facility. Defaults to \code{HospitalName}.
Accepts both raw ESSENCE names and post-\code{\link[janitor:clean_names]{janitor::clean_names()}}
equivalents.}

\item{visit_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted
column name identifying the visit. Defaults to \code{Visit_ID}. Accepts
both raw ESSENCE names and post-\code{\link[janitor:clean_names]{janitor::clean_names()}} equivalents.}
}
\value{
A named list of class \code{essence_dup_summary} with components
\verb{$duplicate_ids}, \verb{$by_facility}, and \verb{$overall}.
}
\description{
Identifies visits with more than one row per facility x Visit_ID combination
and returns a named list with three components: a tibble of duplicated
facility x Visit_ID pairs for filtering or review, a facility-level summary
arranged from most to least duplicated, and an overall count and proportion
of affected visits. Intended to be called on raw or minimally processed
ESSENCE data before \code{\link[=dedupe]{dedupe()}}.
}
\details{
\subsection{Duplicate definition}{

A duplicate is any \verb{facility_col x visit_col} group containing more than
one row. The same \code{Visit_ID} appearing at two different facilities does
not constitute a duplicate -- \code{Visit_ID} is unique only within a facility.
Duplicate detection is therefore always scoped to \verb{facility x visit_col}.
}

\subsection{Return value components}{

\describe{
\item{\verb{$duplicate_ids}}{A tibble of \verb{facility_col x visit_col} pairs
where more than one row exists. Use for targeted review, semi-joins,
or anti-joins prior to deduplication.}
\item{\verb{$by_facility}}{A tibble of facility-level duplicate metrics
arranged from most to least duplicated by \code{n_excess_rows}.}
\item{\verb{$overall}}{A single-row tibble with dataset-level counts and
proportions.}
}
}
}
\examples{
# Basic usage before deduplication
essence_raw |> summarize_duplicates()

# Inspect components
dups <- summarize_duplicates(essence_raw)
dups$overall
dups$by_facility
dups$duplicate_ids

# Filter raw data to duplicated visits for manual review
# (clean first so join keys match snake_case output of $duplicate_ids)
essence_raw |>
  janitor::clean_names() |>
  dplyr::semi_join(
    summarize_duplicates(essence_raw)$duplicate_ids,
    by = c("hospital_name", "visit_id")
  )

}
\seealso{
\code{\link[=classify_duplicates]{classify_duplicates()}} for mechanism-level classification;
\code{\link[=dedupe]{dedupe()}} to remove duplicates after review.
}
