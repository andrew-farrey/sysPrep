% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_treating_geography.R
\name{assign_treating_geography}
\alias{assign_treating_geography}
\title{Assign treating facility geography to out-of-state and OTHER_REGION visits}
\usage{
assign_treating_geography(
  data,
  site = "KY",
  geography = c("region", "zip"),
  region_col = Region,
  hospital_region_col = HospitalRegion,
  zip_col = ZipCode,
  hospital_zip_col = HospitalZip,
  preserve_original_geographies = FALSE,
  clean_names = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame of ESSENCE visit-level records, typically the
output of \code{\link[=dedupe]{dedupe()}} and \code{\link[=filter_care_setting]{filter_care_setting()}}.}

\item{site}{Character string. The site prefix used to identify in-state
visits. Defaults to \code{"KY"}. Visits whose \code{Region} does not begin with
\code{paste0(site, "_")} are treated as out-of-state.}

\item{geography}{Character vector. Which geography types to reassign.
One or both of \code{"region"} and \code{"zip"}. Defaults to
\code{c("region", "zip")}. Types with missing required columns are skipped
with an informative message.}

\item{region_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted
column name for the patient region field. Defaults to \code{Region}.}

\item{hospital_region_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}>
Unquoted column name for the hospital region field. Defaults to
\code{HospitalRegion}. Required when \code{"region"} is in \code{geography}.}

\item{zip_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}> Unquoted
column name for the patient zip code field. Defaults to \code{ZipCode}.
Required when \code{"zip"} is in \code{geography}.}

\item{hospital_zip_col}{<\code{\link[dplyr:dplyr_tidy_select]{tidy-select}}>
Unquoted column name for the hospital zip code field. Defaults to
\code{HospitalZip}. Required when \code{"zip"} is in \code{geography}.}

\item{preserve_original_geographies}{Logical. If \code{TRUE}, adds
\code{original_region} and/or \code{original_zip_code} columns containing
pre-overwrite values before reassignment. Defaults to \code{FALSE}.}

\item{clean_names}{Logical. If \code{TRUE} (default), applies
\code{\link[janitor:clean_names]{janitor::clean_names()}} on output.}

\item{verbose}{Logical. If \code{FALSE}, suppresses informational messages
(\code{rlang::inform()}); warnings and errors are always shown regardless.}
}
\value{
The input data frame with \code{Region} and/or \code{ZipCode} reassigned for
out-of-state and \code{OTHER_REGION} visits. A \code{.out_of_state} logical column
is added indicating which rows were reassigned. When
\code{preserve_original_geographies = TRUE}, \code{original_region} and/or
\code{original_zip_code} columns are also present.
}
\description{
Identifies visits where the patient's recorded \code{Region} does not belong to
the site of interest -- including out-of-state residents and visits with
\code{OTHER_REGION} or missing \code{Region} values -- and replaces their \code{Region}
and/or \code{ZipCode} with the treating hospital's corresponding geographic
fields (\code{HospitalRegion} and/or \code{HospitalZip}). In-state patient visits
are left unchanged.
}
\details{
\subsection{Why this function exists}{

ESSENCE surveillance data includes visits from out-of-state residents and
patients whose legal address is unavailable to the treating facility
(\code{OTHER_REGION}). Simply discarding these visits understates the true
incidence burden experienced by facilities in the surveillance area.
This dropping is rarely a deliberate choice -- a region-based map or
summary table scoped to in-state values will silently exclude these rows
without an explicit filter. EMS-based systems report incidence at the
location of care rather than patient residence; this function brings
ESSENCE data into that same frame. Assigning the treating facility's
geography to these visits retains them in the analytic dataset while
approximating both residence-based and incidence-based burden from a
single geographic field.
}

\subsection{Site prefix detection}{

The \code{Region} field uses the format \verb{\{SITE\}_\{REGION\}}, where \code{SITE} is
the NSSP Site Short Name and \code{REGION} is the ESSENCE Region -- a county
name derived from a zip-code-to-county lookup table maintained by
ESSENCE. Because some site names contain multiple underscores, the
prefix is all characters before the \strong{last} underscore. A visit is
classified as out-of-state or unknown residence when its \code{Region} does
not begin with \code{paste0(site, "_")}, or when \code{Region} is \code{"OTHER_REGION"},
\code{paste0(site, "_UNKNOWN")} (e.g. \code{"KY_UNKNOWN"}), or \code{NA}. Unlike
\code{"OTHER_REGION"}, the \code{"{site}_UNKNOWN"} form carries the site prefix --
ESSENCE uses it for a known-site record whose residential geography
could not be determined, as distinct from a residence that is genuinely
unmapped to any region -- so it is checked explicitly rather than relying
on the prefix mismatch below. For example, with \code{site = "KY"}:
\itemize{
\item \code{"KY_Jefferson"} -> in-state, unchanged
\item \code{"TN_Davidson"} -> out-of-state, region assigned from \code{HospitalRegion}
\item \code{"OTHER_REGION"} -> unknown residence, region assigned from \code{HospitalRegion}
\item \code{"KY_UNKNOWN"} -> unknown residence, region assigned from \code{HospitalRegion}
}
}

\subsection{Geography types}{

Both \code{"region"} and \code{"zip"} are attempted by default. If the columns
required for a geography type are absent, that type is skipped with an
informative message. Specify \code{geography = "region"} or \code{geography = "zip"}
explicitly to suppress messages about the other type when columns are
intentionally absent.
}

\subsection{Hybrid geography assignment and cluster detection}{

This function implements a hybrid geography assignment strategy: in-state
patient residential geography is preserved unchanged, while out-of-state
and \code{OTHER_REGION} visits are attributed to the treating facility's
geography rather than discarded. This matters for cluster detection: a
geographically concentrated surge of out-of-state overdoses at a single
facility -- common near state borders or in tourist destinations -- will
appear in facility-level and county-level signals only if those visits
are attributed to the treating location. Discarding them suppresses the
signal entirely.

Because residential geography is preserved for the in-state majority,
the resulting dataset behaves similarly to what most ESSENCE practitioners
expect from a standard query, with out-of-state and \code{OTHER_REGION} visits
retained rather than dropped. This makes \code{assign_treating_geography()} the
more conservative of the two geography assignment methods in this package
and the recommended starting point for most surveillance applications.
This approach is implemented at scale in Kentucky's statewide syndromic
overdose alert system.

The tradeoff is mixed semantics in the \code{Region} column: it represents
residential geography for most rows and treating-location geography for
reassigned rows. Analytic notes should document this when reporting rates.
}

\subsection{ESSENCE Region field}{

\code{Region} is a formal ESSENCE geographic entity mapped relationally to a
county-to-zip code lookup table in the NSSP BioSense database, representing
the patient's approximated county of residence. It is the preferred geography
for county-level burden estimation. Custom region fields supplied to NSSP by
states (e.g., health planning districts, local health department districts,
community mental health center service areas) are pre-computed from \code{Region}
and would need to be recomputed after reassignment if used downstream.
Because the underlying lookup table is not publicly accessible, this function
does not support reassigning NSSP-site-submitted custom region fields.

\code{Region} is a maintained many-to-one zip-to-county mapping
(\code{Zipcode_to_CountyRegionMapping}), not a live geocode of the record --
zip codes are assigned to a region by centroid by default, but individual
assignments may be overridden by site or state administrators to better
reflect where the bulk of a zip code's population lives. Because of this
approximation, \code{Region}-based results should not be construed as the
authoritative county-level count; they are ESSENCE's standardized
construct for enabling consistent sub-state reporting across data
sources, not ground truth. This function reassigns \code{Region} using
whatever mapping is already present in \code{HospitalRegion} -- it does not
independently geocode or validate that mapping.

\code{essence_raw}/\code{essence_clean} and the examples throughout this package
model an ESSENCE \strong{patient location} pull (\code{va_er}), where \code{Region}
reflects patient residence for the jurisdiction of interest. In a
\strong{facility location} pull (\code{va_hosp}), the population represented
includes both residents and visitors who presented for care at
in-jurisdiction facilities -- \code{assign_facility_geography()} is the
more natural fit for that data source, since it scopes every row to
where care was received rather than mixing residence-based and
visitor rows under one semantic.
}

\subsection{Preserved geographies}{

When \code{preserve_original_geographies = TRUE}, \code{original_region} and/or
\code{original_zip_code} columns are added before overwriting, retaining
pre-assignment values for QA, audit trails, or residential vs. treating
geography comparisons.
}
}
\examples{
# Default: reassign both region and zip for out-of-state visits
essence_clean |> assign_treating_geography()

# Region only -- ZipCode not in pull
essence_clean |> assign_treating_geography(geography = "region")

# Non-Kentucky site
essence_clean |> assign_treating_geography(site = "OH")

# Preserve original values for QA
essence_clean |>
  assign_treating_geography(preserve_original_geographies = TRUE) |>
  dplyr::filter(region != original_region)

# Full pipeline
essence_raw |>
  dedupe(order_by = Arrived_Date_Time) |>
  filter_care_setting() |>
  assign_treating_geography(preserve_original_geographies = TRUE)

}
\seealso{
\code{\link[=assign_facility_geography]{assign_facility_geography()}} to assign facility geography to
all visits regardless of patient residence.
}
